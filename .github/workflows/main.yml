name: Wails build

on:
  push:
    branches:
      - main
    tags:
      - '*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  NODE_OPTIONS: "--max-old-space-size=4096"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        build:
          - name: 'pic-viewer'
            platform:  'linux/amd64'
            os: 'ubuntu-latest'
            artifact-name: 'linux-amd64'
          - name: 'pic-viewer'
            platform:  'linux/arm64'
            os: 'ubuntu-22.04-arm'
            artifact-name: 'linux-arm64'
          - name: 'pic-viewer'
            platform:  'windows/amd64'
            os: 'windows-latest'
            artifact-name: 'windows-amd64'
          - name: 'pic-viewer'
            platform:  'windows/arm64'
            os: 'windows-latest'
            artifact-name: 'windows-arm64'
          - name: 'pic-viewer'
            platform:  'darwin/universal'
            os: 'macos-latest'
            artifact-name: 'macos-universal'

    runs-on: ${{ matrix.build.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build wails
        uses: dAppServer/wails-build-action@main
        id: build
        with:
          build-name: ${{ matrix.build.name }}
          build-platform: ${{ matrix.build.platform }}
          package: false
          go-version: '1.25'

      - name: Rename and Package Assets
        shell: bash
        run: |
          cd build/bin
          for f in *; do
            if [ -d "$f" ]; then
              if [[ "$f" == *.app ]]; then
                zip -r "${f%.app}-${{ matrix.build.artifact-name }}.app.zip" "$f"
                rm -rf "$f"
              fi
            elif [ -f "$f" ]; then
              ext="${f##*.}"
              base="${f%.*}"
              if [ "$ext" = "$f" ]; then
                # No extension (Linux binary)
                mv "$f" "${f}-${{ matrix.build.artifact-name }}"
              else
                # Has extension (.exe, .deb, etc)
                # Avoid double naming if it already contains the platform
                if [[ "$f" != *"${{ matrix.build.artifact-name }}"* ]]; then
                  mv "$f" "${base}-${{ matrix.build.artifact-name }}.${ext}"
                fi
              fi
            fi
          done

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.build.artifact-name }}
          path: build/bin/*


  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded artifacts
        run: ls -R artifacts

      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          files: |
            artifacts/**/*
          generate_release_notes: true
          draft: false
          prerelease: false
